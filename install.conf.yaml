- clean: ['~']

- shell:
    -
        command: ./install -c install.conf.core.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true
    -
        command: ./install -c install.conf.brew.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true
    -
        command: ./install -c install.conf.npm.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true
    -
        command: ./install -c install.conf.vscode.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true
